# Blockchain
A basic implementation of a Blockchain similar to that used in Bitcoin and other Cryptocurrencies. Rather than a currency it uses text messages as transactions. And is only using a SHA-1 schema rather than 256 as this is a project not implementation for a secure application.
